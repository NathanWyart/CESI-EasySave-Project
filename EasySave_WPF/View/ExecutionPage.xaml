<UserControl x:Class="EasySave_WPF.View.ExecutionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="🚀 Exécution des travaux de sauvegarde"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0 0 0 20"
                   HorizontalAlignment="Center"/>

        <!-- Boutons -->
        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    Margin="0 10 0 20">
            <Button Content="Exécuter la sélection" Width="180" Margin="10"/>
            <Button Content="Exécuter tous" Width="180" Margin="10"/>
        </StackPanel>

        <!-- Liste des exécutions (statique) -->
        <ListView Grid.Row="2" Margin="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nom" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="Documents"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Statut" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="En cours..." Foreground="OrangeRed"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Progression" Width="300">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="45" Minimum="0" Maximum="100" Height="16"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>

            <!-- 2ème ligne -->
            <ListViewItem>
                <ListViewItem.Content>
                    <StackPanel Orientation="Horizontal" Visibility="Collapsed"/>
                </ListViewItem.Content>
            </ListViewItem>

            <!-- 3ème ligne -->
            <ListViewItem>
                <ListViewItem.Content>
                    <StackPanel Orientation="Horizontal" Visibility="Collapsed"/>
                </ListViewItem.Content>
            </ListViewItem>

        </ListView>
    </Grid>
</UserControl>
