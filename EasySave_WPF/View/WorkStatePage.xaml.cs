using System.Windows.Controls;

namespace EasySave_WPF.View
{
    public partial class WorkStatePage : UserControl
    {
        public WorkStatePage()
        {
            InitializeComponent();
            // DataContext est déjà défini dans le XAML via <UserControl.DataContext>
        }
    }
}
