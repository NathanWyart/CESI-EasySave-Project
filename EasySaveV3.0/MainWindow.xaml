<Window x:Class="EasySaveV3._0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySaveV3._0"
        mc:Ignorable="d"
        Title="EasySave" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#2E3B4E" Orientation="Vertical" Grid.Column="0">
            <TextBlock Text="EasySave" Foreground="White" FontSize="20" FontWeight="Bold" Margin="10"
                       HorizontalAlignment="Center"/>
            <Separator Margin="0,10"/>
            <Button x:Name="HomeButton" Content="Accueil" Background="White" Margin="10, 15" Height="40" Click="HomeButton_Click"/>
            <Button x:Name="ExecutionButton" Content="Exécution" Background="White" Margin="10, 15" Height="40" Click="ExecutionButton_Click"/>
            <Button x:Name="SettingsButton" Content="Paramètres" Background="White" Margin="10, 15" Height="40" Click="SettingsButton_Click"/>
        </StackPanel>

        <ContentControl Grid.Column="1" x:Name="ContentFrame"/>
    </Grid>
</Window>