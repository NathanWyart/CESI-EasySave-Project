<UserControl x:Class="EasySave.View.ExecutionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EasySave.ViewModels"
             mc:Ignorable="d">

	<UserControl.DataContext>
		<vm:ExecutionViewModel />
	</UserControl.DataContext>

	<Grid Margin="20">
		<StackPanel>
            <TextBlock Text="Liste des Travaux" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
			<ScrollViewer VerticalScrollBarVisibility="Auto" Height="400">
				<ItemsControl ItemsSource="{Binding RunningStates}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,5">
								<StackPanel>
									<DockPanel LastChildFill="True">
										<TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" DockPanel.Dock="Left" />
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                                            <Button Content="⏸️" ToolTip="Pause"
											Command="{Binding DataContext.PauseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
											CommandParameter="{Binding Name}" Margin="2"
											Background="Transparent" BorderThickness="0" FontSize="16" Cursor="Hand"/>

                                            <Button Content="▶️" ToolTip="Play"
											Command="{Binding DataContext.ResumeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
											CommandParameter="{Binding Name}" Margin="2"
											Background="Transparent" BorderThickness="0" FontSize="16" Cursor="Hand"/>

                                            <Button Content="❌" ToolTip="Cancel"
											Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
											CommandParameter="{Binding Name}" Margin="2"
											Background="Transparent" BorderThickness="0" FontSize="16" Cursor="Hand"/>
                                        </StackPanel>

                                    </DockPanel>

									<TextBlock Text="{Binding Progress, StringFormat=Progression : {0}%}" Margin="0,5,0,0"/>
									<ProgressBar Minimum="0" Maximum="100" Height="20" Value="{Binding Progress}" Margin="0,0,0,5"/>
									<TextBlock Text="{Binding StateStatus}" Margin="0,0,0,5"/>
								</StackPanel>
							</Border>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</ScrollViewer>
		</StackPanel>
	</Grid>
</UserControl>